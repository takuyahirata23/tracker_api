defmodule TrackerWeb.FallbackController do
  use TrackerWeb, :controller

  # def call(conn, {:error, :bad_request, %Ecto.Changeset{} = changeset}) do
  #   conn
  #   |> put_status(400)
  #   |> render("invalid_form.json", changeset: changeset)
  # end
end
